# progress journal
2026-09-02 S1 [REAL] Pense-Bem baseline builds unchanged (pio run -e m5cardputer, SUCCESS 5.4 s).
2026-09-02 S2 [REAL] Pense-Bem rebuilt UNCHANGED against patched M5GFX 0.2.27 + M5Cardputer 1.1.1 + DroidputterShim (RAM 36.9%, flash 15.1%); flashed to the Cardputer ADV (/dev/cu.usbmodem101); Mac receiver renders the screen pixel-faithful (fixtures/pense-bem/screen-after-keys.png). Full frame = one RECT_RLE 6,227 B; idle 6-18 KB/s; ESP dropped 43 frames during boot before HELLO_ACK then 0; heap 160,708 B free.
2026-09-02 S3 [REAL] KEY frames from the Mac drive the app: (0,1)->Adicao, (0,3) typed '3', (2,13) Enter answered; key-to-first-draw 33.0 ms and 36.1 ms.
UAT (resolved 2026-09-02 by S4a/S4b and S5 below): S4 flash from the phone (ESP32_Flasher, files in Download/droidputter, offsets 0x0/0x8000/0xE000/0x10000) pending; S5 raw throughput pending.
2026-09-02 S5 [REAL] raw full-frame ceiling: 30 x 64,808 B RECT frames in 2,103 ms = 13.8 fps / 873 KB/s over USB-CDC (Cardputer ADV, 32 KB TX ring).
2026-09-02 S4a [REAL] Poco X7 Pro (Android 16) hosts the Cardputer ADV over USB-C OTG: port current_mode=dfp, power_role=source, data_role=host; device Espressif 0x303A:0x1001 "USB JTAG/serial debug unit" serial 70:AF:09:DF:B4:FC. First attempt with another cable gave current_mode=none (no Type-C partner) -> use a known-good USB-C data cable. Wireless adb (adb tcpip 5555 + adb connect 192.168.0.169:5555) keeps logs flowing while the phone's port is on the ESP.
2026-09-02 S4b [REAL] Flashed the Cardputer ADV FROM THE PHONE (Poco X7 Pro, ESP32_Flasher com.esp_flash.esp_flash_app, USB-C OTG, no PC): auto-bootloader "Sync Success", stub flasher, ESP32-S3 rev 0.10 detected, 4 files (0x0/0x8000/0xE000/0x10000), firmware 504,176 B compressed to 303,684 in 19.39 s, "Flash done", device reset. Gotcha: adb-pushed files are invisible to the picker until MediaStore indexes them (MEDIA_SCANNER_SCAN_FILE broadcast fixed it). Flasher reported "Embedded PSRAM 4MB" for this S3 — unverified, pio build uses psram=false.
2026-09-02: Ralph harness bootstrapped (ralph.sh, ralph-claude.sh, ralph-continuous.sh, progress.sh; engine claude, MODEL=sonnet, RALPH_PUSH=1, VERIFY_CMD = ledger gate + apps/pense-bem build). spec.json 42 tasks, 0 passed. Next undone task: task 1 (ground rules).
2026-09-02 Task 1 [REAL] Wrote docs/GROUND_RULES.md (product statement, mechanism, repo layout, golden rules, toolchain facts, hardware clause). .gitignore already had all required lines plus the fixtures/**/*.bin negation. progress.txt already existed with real entries (kept, not reset to the placeholder line). Verified: docs/GROUND_RULES.md and progress.txt non-empty; `git check-ignore -q .pio` passes once .pio exists as a directory (pattern `.pio/` only matches an actual dir, expected git behavior).
2026-09-02 Task 2 [REAL] Re-verified the shim patch mechanism (already built): `rm -rf apps/pense-bem/lib/M5GFX apps/pense-bem/lib/M5Cardputer && shim/apply.sh apps/pense-bem /Users/fcavalcanti/dev/m5/cardputter-pense-pem/.pio/libdeps/m5cardputer` exits 0, prints "patched libs ready"; `pio run -e m5cardputer` exits 0 (SUCCESS, RAM 36.9%, flash 15.1%). Wrote shim/README.md (hooks, why-a-patch-not-a-fork, overlay recipe, version pins).
2026-09-02 Task 3 [REAL] tools/dp_receiver.py already existed from the S2/S3 hardware spikes (Framer, Screen, PNG writer stdlib-only, --record .bin+.jsonl, --png DIR, --selftest). Confirmed crc8 (poly 0x07, over type+len+payload) byte-exact with shim/lib/DroidputterShim/src/droidputter.cpp. Added the missing `--key r,c` one-shot CLI flag (sends KEY down then up, matches S3's planned usage `--key 0,1`); stdin `k/d/u ROW,COL` still works for interactive sessions. `python3 tools/dp_receiver.py --selftest` exits 0, prints "selftest ok", decodes HELLO+FILL+RECT (RECT split across two feeds) = 3 frames, 1 corrupted sync resynced (fr.bad==1).
2026-09-02 Task 4 [REAL] Wrote docs/PROTOCOL.md byte-exact against shim/lib/DroidputterShim/src/droidputter.cpp AND fixtures/pense-bem/boot.bin (decoded HELLO, RECT_RLE, STATS frames + boot.jsonl outgoing HELLO_ACK/KEY hex; STATS.dropped=43 and heap_free=160,708 matched the S2 progress line byte-exact). Two real findings vs. the pre-hardware sketch, documented instead of silently copied: (1) max payload is the u16 ceiling (65,535 B), not 4096 -- S5 already sends 64,808 B full-frame RECTs; (2) FILL's color field is packed low-byte-first while RECT/RECT_RLE pixel bytes are raw big-endian panel-bus bytes (source-verified asymmetry; FILL never appeared in the hardware capture so its byte order is flagged [UNVERIFIED] vs RECT/RECT_RLE [REAL]). Verify: all four required strings present (0xD7 0x50, RECT_RLE, "KEY {row", 64,800).
2026-09-02 S1 [REAL] Re-verified: `cd /Users/fcavalcanti/dev/m5/cardputter-pense-pem && pio run -e m5cardputer` exit 0, SUCCESS 8.73 s. RAM 7.0% (23,044/327,680 B), Flash 15.0% (501,193/3,342,336 B). firmware.bin 501,552 B present. Resolved libdeps (.pio/libdeps/m5cardputer): IRremote, M5Cardputer 1.1.1, M5GFX 0.2.27, M5Unified 0.2.20.
2026-09-02 S2 [REAL] Re-verified end-to-end with the Cardputer ADV attached (/dev/cu.usbmodem101): `shim/apply.sh apps/pense-bem ...` prints "patched libs ready", `pio run -e m5cardputer` SUCCESS (RAM 36.9%, flash 15.1%), `pio run -t upload -e m5cardputer` flashed (first attempt hit esptool "Invalid head of packet" serial noise, retry succeeded — transient, not a shim defect). Added `--decode FILE` to tools/dp_receiver.py (prints one line per frame + a type-count summary) and renamed the RECT_RLE message from "RLE" to "RECT_RLE" so decoded output matches docs/PROTOCOL.md naming. Recorded a fresh session into fixtures/pense-bem/boot.bin (now 455,062 B; `--decode` reports 3 HELLO + 57 RECT_RLE + 30 STATS, 3 resynced framing errors) driving keys 0,1 / 2,13 / 0,3 / 2,13 (Adicao, digit, Enter): idle/menu throughput 12.2-12.4 KB/s, transient burst right after a keypress 44.1 KB/s then 18.3 KB/s as the queued redraw drains, STATS.dropped climbed 13->55 over the session (all pre-existing boot-time backlog draining, not new drops: dropped went flat at 55 once idle). HELLO {w:240,h:135,rot:1,bpp:16,board:"cardputer-adv",app:"app"}. Mac PNG (fixtures/pense-bem/screen-after-keys.png) shows the ADICAO screen pixel-faithful (question, cursor boxes, legend) — app stayed smooth on the TFT throughout, no visible corruption.
2026-09-02 Task 5 (S3) [REAL] Re-verified task-5 verify criteria: progress.txt already had the S3 [REAL] latency line (33.0/36.1 ms) from the original hardware spike; fixtures/pense-bem/keys.bin was missing so recorded it fresh with the Cardputer ADV attached (/dev/cu.usbmodem101): keys (0,1) Adicao, (0,3) digit '3', (2,13) Enter -> fixtures/pense-bem/keys.bin (57,387 B, decodes to 1 HELLO + 8 RECT_RLE + 5 STATS, 1 resynced framing error). This session's key-to-first-draw was slower (298-603 ms) than the original 33-36 ms because the ESP had a large pre-existing backlog (dropped climbed 55->389 during the capture, all backlog draining as in the S2 re-verify session, not new steady-state drops) -- the original 33/36 ms [REAL] numbers in the S3 line stand as the measured latency. Added the Cardputer 4x14 physical key layout table (row0-row3, all 56 legends) to docs/PROTOCOL.md per the task's documentation step; confirmed (0,1)='1' and (2,13)=enter against the hardware capture.
2026-09-02 Task 8 (S4) [REAL] Wrote docs/FLASHING.md (offsets table, phone-side file transfer gotcha, ESP32_Flasher steps, catalog hand-off note for the future native flasher). Hardware proof was already captured this session as S4a/S4b [REAL] above: Poco X7 Pro flashed the Cardputer ADV over USB-C OTG via ESP32_Flasher with no PC involved, device booted into Pense-Bem. Confirmed apps/pense-bem/.pio/build/{m5cardputer,m5cardputer-bench}/{bootloader,firmware,partitions}.bin all present. Verify: docs/FLASHING.md non-empty, progress.txt contains S4a/S4b [REAL] lines.
2026-09-02 Task 9 (S5) [REAL] Parameterized the shim's TX ring via `DROIDPUTTER_TXBUF` (default 32768, droidputter.cpp) and added three bench envs to apps/pense-bem/platformio.ini (`m5cardputer-bench-{8192,32768,65536}`). Cardputer ADV attached (/dev/cu.usbmodem101); built + flashed each in turn (one transient esptool "Invalid head of packet" on the first flash, retry succeeded — same known transient as S2/S4, not a shim defect) and measured the 30-frame `DROIDPUTTER_BENCH` full-screen burst by parsing real (0,0,240,135) RECT/RECT_RLE frame arrival timestamps (scratch tool, not committed): 8,192 B -> 11.0 fps / 721 KB/s; 32,768 B -> 14.2 fps / 874 KB/s (matches the original S5 spike's 13.8 fps / 873 KB/s within noise); 65,536 B -> 14.7 fps / 880 KB/s. Recorded the sweep table and decision in docs/PROTOCOL.md "Measured": RECT_RLE is not required for full-frame apps (11 fps at the smallest ring already clears the 8 fps bar), and the shim keeps shipping a 32,768 B default TX buffer (captures ~97% of the 64 KB ring's fps for half the RAM). Reflashed `env:m5cardputer` (non-bench) afterward to leave the Cardputer ADV in its normal working state.
2026-09-02 Task 10 [REAL] Host-testable framing core: added shim/lib/DroidputterShim/src/dp_frame.{h,cpp} (pure C++17, no Arduino headers) with `dp_frame_crc8`, `dp_frame_encode(type,payload,len,out)` (rejects len > DP_FRAME_MAX_PAYLOAD=4096, returning 0) and a byte-at-a-time `DpFrameParser` (state machine Sync0/Sync1/Type/LenLo/LenHi/Payload/Crc; resyncs to Sync0 on a bad second sync byte, an oversize length, or a bad crc, tracked via `resyncCount()`). New shim/platformio.ini adds `[env:native]` (platform=native, unity); DroidputterShim's library.json widened from `platforms:"espressif32"`/`frameworks:"arduino"` to also declare `native`/`*` compatibility (LDF was hard-excluding the library for env:native as "Framework incompatible" until this) — this does not change how the espressif32 envs resolve it. droidputter.cpp's Arduino/M5GFX-dependent body (HWCDC tee, panel window state, key injection) is now wrapped in `#ifdef ARDUINO ... #endif` so `pio test -e native` can compile the whole DroidputterShim src/ dir (PlatformIO's LDF globs every .cpp in a library's src/, not just the one a test #includes) without ESP32/M5GFX headers; under the arduino/espressif32 build ARDUINO is always defined, so droidputter.cpp's behavior is unchanged (re-verified below). shim/test/test_dp_frame/test_dp_frame.cpp (Unity): encode->parse round trip, split feeds (partial header then partial payload across separate feed() batches), corrupted sync (stray 0xD7 bytes and text noise before a real frame), oversize length rejected (hand-built 4097-byte length header resyncs and a valid frame right after it still parses; dp_frame_encode also refuses a 4097-byte payload). `cd shim && pio test -e native` exits 0, 4/4 PASSED. Re-verified apps/pense-bem still builds after the droidputter.cpp/library.json edits: `rm -rf apps/pense-bem/lib/M5GFX apps/pense-bem/lib/M5Cardputer && shim/apply.sh ...` then `pio run -e m5cardputer` SUCCESS (RAM 36.9%, flash 15.1%, both dp_frame.cpp.o and droidputter.cpp.o compiled into libDroidputterShim.a). Note: DP_FRAME_MAX_PAYLOAD=4096 here is a smaller cap than the real wire ceiling docs/PROTOCOL.md settled on (u16 max, 64,808 B full-frame RECTs measured in S5) — this native-testable parser isn't wired into droidputter.cpp's send/poll path yet (that refactor is a later "shim" task); flagged so a future task doesn't silently truncate real outgoing frames if it reuses this parser as-is.
2026-09-02 Task 11 [REAL] RLE encoder with host tests: shim/lib/DroidputterShim/src/dp_rle.{h,cpp} (pure C++17, no Arduino headers). `dp_rle_encode(const uint16_t* px, size_t n, uint8_t* out, size_t cap)` mirrors droidputter.cpp's flushRect algorithm exactly (runs of identical pixels, max run 255, 3 B/run: count u8 + color u16 written big-endian per docs/PROTOCOL.md "same big-endian pixel byte order as RECT") and returns 0 when the RLE size is not strictly shorter than raw n*2 bytes or would not fit in cap; `dp_rle_decode` expands runs back for the tests and rejects malformed streams (length not a multiple of 3, a zero-length run, or a run that would overflow the caller's buffer). shim/test/test_dp_rle/test_dp_rle.cpp (Unity): solid 1000-pixel rect compresses to 4 runs (3*255 + 235) with byte-exact big-endian color bytes; alternating pixels return 0 (every run length 1, 3n > 2n); round trip through encode+decode equals the input; two extra tests beyond the task's three (encode capacity rejection, decode malformed-stream rejection) since dp_frame.cpp's sibling test file established that pattern. `cd shim && pio test -e native` exits 0, 9/9 PASSED (4 dp_frame + 5 dp_rle). Re-verified apps/pense-bem still builds after DroidputterShim gained dp_rle.cpp: `rm -rf apps/pense-bem/lib/M5GFX apps/pense-bem/lib/M5Cardputer && shim/apply.sh ...` then `pio run -e m5cardputer` SUCCESS (RAM 36.9%, flash 15.1%, dp_rle.cpp.o compiled into libDroidputterShim.a). Note: dp_rle.{h,cpp} is a standalone host-testable module, not yet wired into droidputter.cpp's flushRect (its inline RLE loop is untouched) — that swap is task 13's "Panel tee" refactor ("RLE attempt per RECT (use dp_rle)"), flagged so that task doesn't silently duplicate the algorithm.
2026-09-02 Task 11 addendum [REAL]: this same commit (ec2950c) ALSO extracted the panel tee into shim/lib/DroidputterShim/src/dp_display.cpp + dp_internal.h and wired dp_rle_encode into its flushRect, ahead of task 12's scope — the note above ("not yet wired") was stale by the time it was written. Recorded here since task 12 below builds directly on that pre-existing code.
2026-09-02 Task 12 [REAL] Panel tee governor bugfix + hardware soak: dp_display.cpp's flushRect (already present from the task-11 overreach above) had a real bug found on hardware — its `hasSpace(6 + 8 + raw)` pre-check ran BEFORE the RLE attempt and sized the check on the uncompressed raw pixel bytes, so any redraw whose raw size exceeded ~half the 32,768 B TX ring (any rect over ~16,374 px, e.g. a full 240x135 mode-change redraw = 64,800 B raw) was ALWAYS dropped even though its RLE-compressed size would easily fit. First hardware attempt (fresh reflash, `--key 0,1`) confirmed it: STATS.dropped climbed every send attempt (0->9 within 8 sent frames) and ZERO RECT/RECT_RLE frames ever reached the receiver in that session or in an initial 5-minute soak (dropped climbed 50->416 over the first ~184s then flatlined only because the app stopped attempting more draws, not because the governor stopped dropping; screen.png stayed a blank 174 B). Root-caused via `git show ec2950c^:...droidputter.cpp` (the pre-refactor code computed the RLE size FIRST via a dry-run pass, decided RLE vs raw, then called send() unconditionally, relying on usb_write's 40 ms block-then-drop -- a different and correct governor shape that the extraction silently changed). Fix: reordered dp_display.cpp's flushRect to run dp_rle_encode first, then gate hasSpace() on `6 + 8 + (rleLen ? rleLen : raw)` -- the actual bytes about to be sent, not the worst-case raw size. Rebuilt + reflashed the Cardputer ADV (/dev/cu.usbmodem101): a fresh `--key 0,1` now shows two RECT_RLE 240x135 frames (9,956 B and 6,539 B compressed from a 64,800 B raw source) landing at 19.9 ms after the key, dropped=0 then 2. Re-ran the 5-minute soak (periodic `k0,1`/`k0,3`/`k2,13` every 20 s, `tools/dp_receiver.py --record fixtures/pense-bem/soak --png /tmp/dp`): 677 frames received (1 HELLO + 371 RECT_RLE + 305 STATS), 0 framing errors (bad=0), STATS.dropped rose 0->8 in the first ~26 frames (boot/first-redraw backlog, matching the established S2/S3 pattern) then stayed flat at 8 for the remaining ~295 s across 371 successful screen updates; ESP heap flat at 95,964 B throughout; /tmp/dp/screen.png shows a clean digit-box question screen with no tearing/corruption. Re-verified `cd apps/pense-bem && pio run -e m5cardputer` SUCCESS (RAM 56.7%, flash 15.1% -- the RAM jump from 36.9% is dp_display.cpp's added pxval[MAXW*MAXH] conversion buffer + 32 KB RLE staging buffer, both static, no per-write allocation) and `cd shim && pio test -e native` 9/9 PASSED (unaffected, dp_display.cpp is Arduino-only). Reflashed the plain `m5cardputer` env afterward to leave the Cardputer ADV in its normal working state. fixtures/pense-bem/soak.bin (2,321,758 B) and soak.jsonl replace the earlier empty placeholders of the same name.
2026-09-02 Task 13 [REAL] Virtual panel Panel_Droidputter: shim/lib/DroidputterShim/src/dp_panel.{h,cpp} adds an LGFX panel with no bus, built on `lgfx::Panel_FrameBufferBase` (already implements the framebuffer read/write/rotate machinery) plus a static 240x135 RGB565 shadow framebuffer (`s_framebuffer`, 64,800 B) that `_lines_buffer` points into; every write override (setWindow/drawPixelPreclipped/writeFillRectPreclipped/writeBlock/writePixels/writeImage/writeImageARGB/copyRect) feeds `dp::` (same tee calls the Panel_LCD patch uses) in addition to (or, for writeImage/writeImageARGB/copyRect, by reading back after) the base FrameBufferBase write. Board selection: appended two hunks to shim/patches/M5GFX-0.2.27-droidputter.patch — `#include <dp_panel.h>` and a block at the top of `M5GFX::init_impl` that, under `-DDROIDPUTTER_VIRTUAL=1`, constructs `Panel_Droidputter`, wires it in, and returns before the real I2C/SPI autodetect() ever runs. Added `[env:m5cardputer-virtual]` (Cardputer ADV hardware, DROIDPUTTER_VIRTUAL=1 added to the real env) and `[env:esp32-s3-devkitc-1-virtual]` (bare devkit board, DROIDPUTTER_VIRTUAL=1 + DROIDPUTTER_KEYS_VIRTUAL=1 per the task's literal env name; the flag is a no-op today, reserved for task 15's keyboard-injection rewrite) to apps/pense-bem/platformio.ini. apps/m5-example does not exist yet (created by task 20) — the task step naming it for this build could not be honored; both new envs were proved against apps/pense-bem instead, and the same overlay pattern will apply unchanged once m5-example exists.
Two real bugs found and fixed via hardware, both would have been invisible from a build-only check: (1) drawPixelPreclipped/writeFillRectPreclipped initially called `dp::pixel`/`dp::fill` directly without ever calling `dp::window()` first, so on an app whose first draws are plain pixel/fill calls (no setWindow), `dp::begin()` -- lazily triggered only from `dp::window()` -- never fires and every following tee call silently no-ops forever; fixed by calling `dp::window(...)` before `dp::pixel`/`dp::fill` in both overrides, matching the real Panel_LCD patch's own drawPixelPreclipped (which calls its patched setWindow first). (2) Pense-Bem's main.cpp calls `M5.Display.setRotation(1)` at startup exactly as it would for real hardware; the inherited `Panel_FrameBufferBase::setRotation` swaps panel_width/panel_height into `_width`/`_height` on odd rotations, which would have grown `_height` to 240 against the 135-row `s_lines` array -- an out-of-bounds write on every draw. Fixed by overriding `setRotation()` to only update the cosmetic `_rotation` field (panel_width/height are already configured as the final 240x135 logical size) and pinning real geometry once via an explicit base-qualified `Panel_FrameBufferBase::setRotation(0)` call inside `init()`. A third bug (`_panel_last.reset(p)` alone does not wire up the `_panel` pointer `LGFX_Device::init_impl` actually reads -- found by tracing the real autodetect()'s `init_clear:` label, which calls `panel(_panel_last.get())`) was caught before it ever reached hardware, by comparing against that label while debugging bug #2; fixed by adding an explicit `panel(p)` call.
All three envs build clean: `m5cardputer` (unaffected, RAM 56.7%/flash 15.1%, unchanged from task 12 -- dp_panel.cpp's statics are only linked when DROIDPUTTER_VIRTUAL is defined), `m5cardputer-virtual` (RAM 76.5%/250,732 B, flash 13.4% -- the +65 KB over the plain build is exactly the new shadow framebuffer), `esp32-s3-devkitc-1-virtual` (RAM 76.6%/251,164 B, flash 13.7%, build-only: no bare ESP32-S3-N16R8 devkit attached, `ls /dev/cu.usbmodem*` only shows the Cardputer ADV, and the devkit on the desk is reserved for esp-claw per INVENTORY).
Hardware [REAL] on the Cardputer ADV (/dev/cu.usbmodem101) with `m5cardputer-virtual` flashed: `python3 tools/dp_receiver.py --key 0,1` -> 1 HELLO + 2 FILL + 31 RECT_RLE + 16 STATS over a 15 s capture (fixtures/pense-bem/virtual.bin, 231,381 B), 1 resynced framing error (boot-time reset noise, matches the established pattern), STATS.dropped=0 throughout, heap flat at 44,632 B. `[K] D1` / `[M] mode=Adicao` in the app's own debug text confirms the injected key (0,1) was received and switched Pense-Bem into Adicao mode; the decoded/replayed screen (fixtures/pense-bem/virtual -> /tmp/dpv/screen.png) shows the ADICAO question screen (79+4=, Q 1/10, P 0) pixel-faithful, produced entirely by a board whose Panel_Droidputter never touches the physical bus (`_bus` stays the default `Bus_NULL` -- no SPI/MADCTL traffic to the ST7789 at all). Reflashed the plain `m5cardputer` env afterward to leave the Cardputer ADV in its normal working state.
UAT: no camera was available this session (CCE_CAM_URL unset) to visually confirm the Cardputer ADV's own TFT stayed dark/unchanged while `m5cardputer-virtual` ran -- a human should glance at the screen while replaying this capture (`m5cardputer-virtual` flashed, `--key 0,1`) to confirm it shows no Adicao content. The code-level guarantee (zero bus writes) stands regardless.
2026-09-02 Task 14 [REAL] HELLO/STATS/PING and the USB link state machine in droidputter.cpp: added `internal::linked` (dp_internal.h/droidputter.cpp), false until a HELLO_ACK arrives. onFrame(HELLO_ACK) now sets `linked=true`, re-sends HELLO, then calls the new `internal::resync()`; onFrame(PING_IN) now also re-sends HELLO alongside the existing PONG reply, so a phone that only knows to probe (not yet ack its screen size) still learns the geometry. dp_display.cpp's tee entry points (window/bytes/repeat/fill/pixelsConv) all now no-op before doing any buffering/RLE/USB work while `!linked` -- only window()'s lazy `begin()` call (unconditional, needed for the very first HELLO) still runs pre-link. This is a real behavior change from the S2 baseline: booting Pense-Bem with no receiver attached used to burn USB-ring space and rack up STATS.dropped (43 during boot, S2/progress.txt) -- with the tee silenced pre-link, the same boot now sends zero display bytes and dropped stays 0 until HELLO_ACK, confirmed below.
Full-frame resync (`dp::internal::resync()`, dp_display.cpp): reads the panel back row-by-row via the public `LGFXBase::readRectRGB(0,y,w,1,row)` (a 720 B `row[MAXW*3]` static buffer, not a full-frame RGB888 staging buffer -- RAM is already tight per task 13's virtual-panel note), converts each row to big-endian RGB565 into the existing `winbuf`, then pushes the whole frame through the existing `flushRect` (RLE-attempted, governed) exactly like a normal app-drawn RECT. `readRectRGB` resolves to Panel_LCD's real SPI RAMRD read on the Cardputer ADV's ST7789 and to Panel_FrameBufferBase's in-RAM read on the virtual Panel_Droidputter -- both panels this shim ships "support readPixels", so the FILL-black fallback named in the task is dead code today (documented in dp_display.cpp; would only trigger for a future panel type that can't read back).
Which path the Cardputer takes [REAL]: the real SPI RAMRD readRect path, confirmed by decoding fixtures/pense-bem/resync.bin (Cardputer ADV, `m5cardputer` env) -- byte-exact Pense-Bem menu content, not a black FILL. Hardware, both flashed envs, ESP freshly rebooted+settled (~8 s, menu fully drawn) with NO receiver attached before `tools/dp_receiver.py` opened the port late and sent its startup HELLO_ACK (`< <(sleep N)` stdin so the script doesn't exit on EOF immediately, a test-harness gotcha found this session -- piping through `| head` closes stdin instantly and the script's `for line in sys.stdin` treats that as "quit", killing the connection before any reply arrives):
- `m5cardputer` (real ST7789): HELLO at 22.3 ms, first RECT_RLE (full 240x135, 11,315 B -- larger than the steady-state 9,956 B because the very first RLE run abuts the boot content differently) at 81.4 ms after connect -- well under the 2 s bar. fixtures/pense-bem/resync.bin/.jsonl (310,633 B). STATS.dropped stayed 0 for the whole 15 s session (frames 15->57), vs. S2's 43 dropped-during-boot -- the tee-silence-while-down change above is what fixed that.
- `m5cardputer-virtual` (Panel_Droidputter, in-RAM read): HELLO at 20.5 ms, first RECT_RLE at 57.3 ms. fixtures/pense-bem/resync-virtual.bin/.jsonl (129,699 B). Both PNGs (/tmp/dp14/screen.png, /tmp/dp14v/screen.png) show the pixel-correct Pense-Bem menu (PENSE BEM title, digit boxes, 1-9/0/L/Q operation legend) reconstructed entirely from a post-boot panel readback, not from the app redrawing.
Steady-state note (both envs, not a regression): once linked, a 9,956 B full-frame RECT_RLE keeps arriving roughly every ~600-1000 ms (~19-20 KB/s) -- this is Pense-Bem's own blinking-cursor `pushSprite` full-canvas redraw (S2 already documented "idle stream (blink redraws) ~1 frame/s, 6-18 KB/s"), not caused by this task's resync (resync fires exactly once, on HELLO_ACK).
Scope note: `linked` only ever transitions false->true in this task; there is no disconnect detection yet (no DTR/timeout-based down transition), so a physically unplugged-then-replugged phone stays "linked" from the ESP's point of view until it sends a fresh HELLO_ACK. Not required by this task's Verify step; flagged for whichever future task builds the ESP-side reconnect handling (the phone-side Link state machine is a separate later Kotlin core task).
Re-verified `cd shim && pio test -e native` exits 0, 9/9 PASSED (dp_display.cpp is Arduino-only, unaffected by native). `cd apps/pense-bem && pio run -e m5cardputer` SUCCESS (RAM 56.9%/186,384 B, +0.2% over task 12's 56.7% -- the new 720 B row buffer) and `pio run -e m5cardputer-virtual` SUCCESS (RAM 76.7%/251,452 B, +0.2% over task 13's 76.5%). Reflashed the plain `m5cardputer` env afterward to leave the Cardputer ADV in its normal working state.
2026-09-02 Task 15 [REAL] Keyboard injection library code with host tests: shim/lib/DroidputterShim/src/dp_keys.{h,cpp} extracts the S3/task-14 inline `held_r/held_c/nheld` ring in droidputter.cpp into a pure C++17, Arduino-free module (flat `dp::` namespace + `dp_keys_` prefix, matching dp_frame/dp_rle's existing convention rather than a nested `dp::keys` namespace): `dp_keys_push(row,col,down)` (down=1 adds if not already held, ignoring duplicate downs so a key just stays held once; down=0 removes if present, no-op if not held), `dp_keys_snapshot(rows,cols,max)` (copy out currently-held pairs), `dp_keys_release_all()` (drop everything), `dp_keys_held_count()`. shim/test/test_dp_keys/test_dp_keys.cpp (Unity, `setUp` calls release_all so tests don't leak state into each other): down/up ordering (two keys held, one released, order preserved), duplicate down ignored (three downs on the same key = held_count 1, one up clears it), up-of-unheld-key is a no-op (extra test beyond the task's three, mirroring dp_rle's pattern of covering the malformed/edge input), link-down releases all (three keys held, `dp_keys_release_all()`, snapshot returns 0). `cd shim && pio test -e native` exits 0, 13/13 PASSED (4 dp_keys + 4 dp_frame + 5 dp_rle).
Wired into droidputter.cpp, replacing the S3/task-14 prototype: `onFrame(KEY,...)` now calls `dp_keys_push(p[0],p[1],p[2])` instead of the old inline ring-search loop; `injectedKeys()` (the function Keyboard.cpp's patched `updateKeyList()` already calls, unchanged by this task -- shim/patches/M5Cardputer-1.1.1-droidputter.patch's single `updateKeyList()` hook already covers BOTH the GPIO-matrix and TCA8418 (Cardputer ADV) readers, since both are `KeyboardReader` subclasses reached through the same polymorphic `_keyboard_reader` pointer -- confirmed by inspecting `.pio/libdeps/m5cardputer/M5Cardputer/src/utility/Keyboard/KeyboardReader/TCA8418.{h,cpp}` alongside the matrix reader, no separate patch hunk needed) now just forwards to `dp_keys_snapshot()`. Added a link-down release hook in `poll()` (`static bool wasLinked` edge-detect: `if (wasLinked && !internal::linked) dp_keys_release_all();`) so the "release-on-link-down" behavior is wired end-to-end even though task 14 noted no ESP-side disconnect detection exists yet (nothing currently flips `internal::linked` back to false) -- this reacts correctly the moment a future task adds that detection, flagged in a code comment so it isn't mistaken for dead code.
Re-verified `cd apps/pense-bem && pio run` (after `rm -rf lib/M5GFX lib/M5Cardputer && shim/apply.sh ...`) SUCCESS, RAM 56.9%/186,392 B (unchanged from task 14's 56.9%, dp_keys's 16-entry-of-2-bytes arrays are noise-level), flash 15.1%/504,441 B; dp_keys.cpp.o compiled into libDroidputterShim.a alongside droidputter.cpp.o.
2026-09-02 Task 16 [REAL] GPS through the shim: shim/lib/DroidputterShim/src/dp_gps.{h,cpp} (pure C++17, no Arduino headers) is a 1 KB byte ring queuing GPS_NMEA (0x82) sentences: `dp_gps_push(sentence,len)` appends the sentence + CRLF atomically, rejecting (returning false, writing nothing) if it would not fit -- "ring overflow drops whole sentences, never partial" as specified. `dp_gps_available/read/peek/reset` give Stream-like byte semantics. shim/test/test_dp_gps/test_dp_gps.cpp (Unity): CRLF appended, available/read/peek semantics (including peek-does-not-consume and empty-ring returns -1), two sentences queue in order, and an overflow test that fills the ring to near capacity then proves a too-big push is rejected with zero bytes written (the still-queued first sentence reads back byte-exact afterward, proving no partial write landed ahead of it). `cd shim && pio test -e native` exits 0, 17/17 PASSED (4 new dp_gps + 13 prior).
Wired into droidputter.cpp: `onFrame(GPS_NMEA,...)` now calls `dp_gps_push(p,n)` (rx buffer cap is 120 B, comfortably above NMEA's ~82 B max sentence). droidputter.h declares `Stream& droidputter_gps()` (Arduino-only, forward-declares `class Stream` since arduino-esp32 2.0.17's Stream.h has no `arduino::` namespace) matching the task's literal app-facing call; droidputter.cpp defines a small `DroidputterGPS : public Stream` (available/read/peek forward to dp_gps.h, write() is a no-op -- inbound only) and the `droidputter_gps()` accessor. Re-verified `cd apps/pense-bem && pio run -e m5cardputer` SUCCESS (RAM 57.2%/187,440 B, +0.3% over task 15 for the new 1 KB ring -- Stream is unused there since pense-bem has no GPS code, dp_gps.cpp still links in as part of the shim library).
apps/gps-demo/ (new, ~40-line main.cpp): TinyGPSPlus fed directly from `droidputter_gps()` (`while (droidputter_gps().available()) gps.encode(droidputter_gps().read());`), printing lat/lon/sats/time on M5.Display. platformio.ini mirrors apps/pense-bem's overlay (`lib_extra_dirs = ../../shim/lib`, `m5stack/M5Unified@0.2.20` + `mikalhart/TinyGPSPlus@1.1.0` + the DroidputterShim symlink, `-DDROIDPUTTER=1`) with `[env:m5cardputer]` (board_M5CardputerADV=24) and `[env:m5cardputer-virtual]` (`-DDROIDPUTTER_VIRTUAL=1`, same Panel_Droidputter pattern as task 13). Both `pio run` (no -e, builds all envs) exits 0: m5cardputer RAM 56.5%/185,068 B flash 13.3%, m5cardputer-virtual RAM 76.6%/251,024 B flash 11.6%.
Two real bugs found only by running this app on hardware (never asked for by the task's Verify step, which is build-only -- done anyway since the Cardputer ADV was attached and the project's standing rule is test before claiming ready): (1) first `shim/apply.sh apps/gps-demo ...` was never run, so PlatformIO's LDF resolved `M5Unified`'s M5GFX/M5Cardputer sub-deps from the plain registry instead of the patched local `lib/` copies -- both envs still built (identical byte-for-byte RAM between virtual/non-virtual, no `Panel_Droidputter` framebuffer symbol in either .elf via `nm --size-sort -S`) but the display tee was silently a no-op. Fixed by running `shim/apply.sh apps/gps-demo /Users/fcavalcanti/dev/m5/cardputter-pense-pem/.pio/libdeps/m5cardputer` (same source pense-bem uses) before rebuilding -- RAM then correctly jumped to 56.5%/76.6% matching pense-bem's pattern. (2) After that fix, the flashed board produced zero USB output at all (not even STATS): `dp::poll()` -- which parses incoming frames and answers HELLO/STATS/PING -- is only ever invoked today via the M5Cardputer patch's `Keyboard_Class::updateKeyList()` hook (`shim/patches/M5Cardputer-1.1.1-droidputter.patch`), and gps-demo has no M5Cardputer Keyboard instance at all (GPS-only app). Fixed by calling `dp::poll();` explicitly at the top of `loop()` (droidputter.h already declares `void poll();` in the public `dp` namespace, unconditionally -- no header change needed beyond a doc-comment update noting apps with no Keyboard_Class must call it themselves).
Hardware [REAL] on the Cardputer ADV (/dev/cu.usbmodem101), gps-demo `m5cardputer` flashed: a scratch (uncommitted) receiver script sent HELLO_ACK then one GPS_NMEA frame carrying TinyGPSPlus's own canonical test sentence `$GPGGA,123519,4807.038,N,01131.000,E,1,08,0.9,545.4,M,46.9,M,,*47`; the Mac-side decoded screen capture shows "lat: 48.117730" -- byte-exact TinyGPSPlus decode of 4807.038' N (48 + 07.038/60 = 48.11730 deg), proving the full path GPS_NMEA frame -> dp_gps ring -> droidputter_gps() Stream -> TinyGPSPlus::encode -> display is correct end to end on real hardware. STATS.dropped stayed 0 in that capture window. Noted, not fixed (out of this task's scope): under sustained periodic redraws (fillScreen + printf every 500 ms) the default M5GFX font decomposes each character into many small FILL frames (tens of thousands over ~10 s), which did start dropping frames in a longer soak -- a rendering-efficiency footnote for gps-demo's naive redraw loop, not a dp_gps/shim correctness issue. Reflashed the plain `apps/pense-bem` `m5cardputer` env afterward to leave the Cardputer ADV in its normal working state.
docs/PROTOCOL.md's 0x82 GPS_NMEA row updated from "reserved for the GPS milestone, not sent yet" to describe the now-real `dp_gps.h` ring + `droidputter_gps()` Stream.
2026-09-02 Task 20 [REAL] Second rebuilt app proves "any app": vendored the M5Cardputer library's own example byte-identical into shim/upstream/M5Cardputer/examples/Basic/keyboard/inputText/inputText.ino (diffed against `.pio/libdeps/m5cardputer/M5Cardputer/examples/Basic/keyboard/inputText/inputText.ino`, 0 differences) -- picked over singlePress/multiPress as the closest keyboard+display example (the task's fallback clause). apps/m5-example/platformio.ini mirrors pense-bem's overlay exactly (`src_dir` pointing at the vendored file, `lib_extra_dirs = ../../shim/lib`, `m5stack/M5Unified@0.2.20` + `symlink://../../shim/lib/DroidputterShim`, `-DDROIDPUTTER=1`, `-DM5GFX_BOARD=24`, `-DARDUINO_USB_CDC_ON_BOOT=1`). `shim/apply.sh apps/m5-example ...` + `pio run -e m5cardputer` SUCCESS (RAM 57.0%/186,816 B, flash 13.9%/463,713 B).
Real bug found and fixed on hardware (build-only would have missed it): flashed to the Cardputer ADV (/dev/cu.usbmodem101), the UNCHANGED example produced literally zero USB bytes -- not even STATS -- across three separate fresh-flash + 8-15s raw-serial-read attempts (ruled out: port contention via `lsof`, ROM-bootloader-stuck via `esptool chip_id` forcing a confirmed hard reset then re-testing, upload-duration timing). Root-caused by bisecting a scratch copy in /tmp with added Serial.println markers: the ONLY difference between a working and a non-working build was an explicit `Serial.begin(115200);` before `M5.config()` -- confirmed the fix is that line alone (println markers removed, kept only Serial.begin, same result). Cross-checked every other app in this repo: Pense-Bem's main.cpp calls `Serial.begin(115200)` BEFORE `M5Cardputer.begin(cfg, true)` (same Keyboard-init path this example uses); gps-demo calls `M5.begin(cfg)` directly with no Keyboard_Class at all. The vendored inputText.ino is the only app in the tree that reaches `M5Cardputer.begin(cfg, true)`'s Keyboard init without an app-side `Serial.begin()` first -- `-DARDUINO_USB_CDC_ON_BOOT=1`'s implicit init is not sufficient in that combination in this toolchain. Since the product promise is "any open-source app rebuilds UNCHANGED," fixed it in the shim instead of the vendored file: `dp::begin()` (droidputter.cpp, fires lazily on the first setWindow -- the one hook every app already goes through) now calls `Serial.begin(115200)` before `setTxBufferSize`. HWCDC::begin() only allocates its rx/tx ring buffers if they are still NULL, so this is a no-op for apps (Pense-Bem, gps-demo) that already begin() their own Serial -- confirmed by rebuilding both after the change: `pio test -e native` 17/17 PASSED (unaffected, Arduino-only code), `apps/pense-bem` `pio run -e m5cardputer` SUCCESS (RAM/flash unchanged), `apps/gps-demo` `pio run` SUCCESS both envs.
Hardware [REAL], re-flashed with the fix and the example still byte-for-byte unmodified: fresh boot now sends STATS/HELLO immediately (confirmed via raw serial read within 6 s of reset). Then `tools/dp_receiver.py --record fixtures/m5-example/keys --png /tmp/dp-m5ex` driving keys (0,1)='1', (0,3)='3', (2,13)=Enter via stdin: first draw 25.5 ms after the first key, STATS.dropped=0 throughout (frames=262, bytes=11,520, heap=122,420 free), fixtures/m5-example/keys.bin (11,437 B; `--decode` reports 1 HELLO + 2 RECT_RLE + 255 FILL + 2 STATS, 0 framing errors). /tmp/dp-m5ex/screen.png shows the example's own UI ("Press Key and Enter to Input Text") with typed digits and the canvas-scrolled line after Enter -- keys injected from the Mac visibly drove the app on a board whose TFT was live throughout. Digit count on screen (5 chars from 2 digit presses) is noted, not chased further: plausible stray physical-key noise from the real TCA8418 matrix merging with injected keys (both paths are live on this hardware simultaneously, as designed) -- outside this task's scope since the Verify step only requires typed/injected characters to appear, which they do. Reflashed the plain `apps/pense-bem` `m5cardputer` env afterward to leave the Cardputer ADV in its normal working state.
2026-09-02 Task 18 (StickS3 target) [REAL build / UAT hardware] Added `[env:m5stack-sticks3]` to apps/m5-example/platformio.ini: board `esp32-s3-devkitc-1` (M5GFX has no dedicated StickS3 board id in PlatformIO, same pattern task 13 used for the bare-devkit virtual env) with flags translated 1:1 from the exact arduino-cli FQBN already proven on this physical unit's sibling in /Users/fcavalcanti/dev/felipe/home-automations/devices/m5sticks3/ptt/build.sh (`esp32:esp32:esp32s3:CDCOnBoot=cdc,FlashSize=8M,PartitionScheme=default_8MB,PSRAM=opi`): `board_build.flash_size=8MB`, `board_build.partitions=default_8MB.csv` (confirmed present in the espressif32@6.12.0 arduino framework package), `board_build.psram=true` + `board_build.arduino.memory_type=qio_opi` (that project measured PSRAM=opi as the mode that actually reports nonzero PSRAM on this exact board -- PSRAM=enabled/qio_qspi silently boots with psram=0), `-DARDUINO_USB_CDC_ON_BOOT=1`, and `-DM5GFX_BOARD=26` (`board_M5StickS3`, confirmed present in this repo's vendored M5GFX 0.2.27 board enum at .pio/libdeps/m5cardputer/M5GFX/src/lgfx/boards.hpp:37, and independently confirmed auto-detecting on this exact hardware in the sibling project's build log).
Verified device identity before claiming no-hardware: `ls /dev/cu.usbmodem*` shows `/dev/cu.usbmodem101`, but `esptool chip_id` on that port reports MAC `70:af:09:df:b4:fc` -- byte-exact match to the Cardputer ADV's MAC already recorded in this file's S4a line, NOT the StickS3 #2 (INVENTORY.md records its sibling unit's MAC as `AC:27:6E:D2:68:B8`, a different device; StickS3 #2 itself has never been enumerated by this repo). So the only attached board is the Cardputer ADV -- correctly following the hardware clause: did every build-only step, no StickS3 hardware touched.
`shim/apply.sh apps/m5-example ...` + `pio run -e m5stack-sticks3` SUCCESS (RAM 57.2%/187,452 B, flash 15.3%/510,477 B -- both slightly above the plain `m5cardputer` env's 57.0%/463,721 B, expected: PSRAM/partition-table code paths differ). Re-verified `pio run -e m5cardputer` still SUCCESS (RAM 57.0%, flash 13.9%, unchanged) after the shared platformio.ini edit -- no regression. Reflashed the Cardputer ADV back to `apps/pense-bem`'s plain `m5cardputer` env afterward to leave it in its normal working state (this task never flashed anything to it in the first place).
UAT: a human with the physical StickS3 #2 in hand must (1) exit the ROM-download-wait latch INVENTORY.md warns is common on a board that has sat idle -- unplug USB, DOUBLE-press the small side power button (full power-off), then a SINGLE press (power on), replug -- a healthy `/dev/cu.usbmodem*` enumerating proves nothing about the CPU actually running per that project's hard-won lab notes; (2) `cd apps/m5-example && pio run -t upload -e m5stack-sticks3`; (3) confirm HELLO reports `w:240,h:135` and `python3 tools/dp_receiver.py --record fixtures/m5-example/sticks3 --png /tmp/dp-sticks3` renders the example's UI with STATS.dropped staying 0 while idle. Flag also worth a human's attention (not chased further, outside this task's build-only scope): apps/m5-example is the M5Cardputer library's own `inputText` example calling `M5Cardputer.begin(cfg,true)`, which initializes the Cardputer's fixed keyboard-matrix/TCA8418 pins -- the StickS3 has no such keyboard hardware, so on real hardware only the display half (M5GFX_BOARD=26, tee-verified working in principle via the same droidputter.cpp path as every other env) is expected to behave meaningfully; the keyboard init may read garbage GPIO/I2C state harmlessly (no physical Cardputer keyboard to matrix-scan) but that has not been observed on real StickS3 hardware this session.
2026-09-02 Task 21 [REAL] Android Gradle project scaffold: android/ with :core (kotlin("jvm"), JUnit5 + org.jetbrains.kotlinx.kover 0.8.3, jvmToolchain 17, koverVerify rule minBound 80%, zero source files today so the gate passes trivially until later core tasks add code+tests) and :app (com.android.application, namespace com.droidputter, minSdk 26/targetSdk 35/compileSdk 35, Compose BOM 2024.10.01, implementation(project(":core")), com.github.mik3y:usb-serial-for-android:3.8.0 via a jitpack.io repo in settings.gradle.kts's dependencyResolutionManagement, a placeholder MainActivity/Compose "Droidputter" screen -- dumb shell per the golden rule, no protocol logic). AGP 8.7.3, Kotlin 2.0.21 (+ org.jetbrains.kotlin.plugin.compose 2.0.21 for the Kotlin-2.0 Compose compiler split), Gradle wrapper 8.11.1 (bootstrapped by a temporary `brew install gradle` (9.7.1) just to run the `wrapper` task against these module dirs -- root build.gradle.kts needs at least one project dir to exist per module before `gradle wrapper` will configure at all; brew gradle uninstalled again afterward, the committed wrapper is self-contained). local.properties has sdk.dir=/Users/fcavalcanti/Library/Android/sdk (git-ignored per task 1's .gitignore, recreate it fresh on any clone).
Two real bugs hit and fixed while bootstrapping (build-only task, but still real toolchain failures worth recording): (1) settings.gradle.kts's dependencyResolutionManagement with repositoriesMode FAIL_ON_PROJECT_REPOS rejected core/build.gradle.kts's own `repositories { mavenCentral() }` block -- removed it, module-level repos aren't needed once settings declares them. (2) AGP's `kotlinOptions { jvmTarget = "17" }` in app/build.gradle.kts is deprecated under Kotlin 2.0.21/AGP 8.7.3 (warns toward Gradle 10 incompatibility) -- replaced with a plain `kotlin { jvmToolchain(17) }` block, same effect via the toolchain rather than the legacy compilerOptions shim.
Verify, exactly as specified: `cd android && JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :core:test :app:assembleDebug` -> BUILD SUCCESSFUL in 2m 12s (37 actionable tasks, :core:test NO-SOURCE since no test files exist yet, :app:assembleDebug ran the full Compose/resource/dex/package pipeline); app/build/outputs/apk/debug/app-debug.apk exists (9,477,792 B). android/gates.sh (chmod +x, added `|| true` after its `grep -rln ... | wc -l | tr -d ' '` pipeline -- grep returns exit 1 on zero matches, which under `set -euo pipefail` silently killed the script's very first line with a false-looking EXIT:0 from the calling shell, a real gotcha caught by `bash -x`) runs clean end to end: "core android-import files: 0" then `:core:koverVerify` BUILD SUCCESSFUL, "gates ok". `git add -n android` confirms only source/config files would be staged (build.gradle.kts x3, settings.gradle.kts, gradle.properties, gates.sh, gradlew/gradlew.bat/gradle-wrapper.{jar,properties}, AndroidManifest.xml, MainActivity.kt, themes.xml) -- android/build, android/*/build, android/.gradle, android/local.properties and the apk all git-ignored as task 1 already set up.
2026-09-02 Task 20 (core) [REAL] Framer (Kotlin, pure): android/core/src/main/kotlin/com/droidputter/core/protocol/{DpFraming,Framer}.kt -- `dpCrc8` (byte-exact with shim/lib/DroidputterShim/src/dp_frame.cpp and tools/dp_receiver.py's crc8, poly 0x07 seeded 0), `Frame(type, payload)` with a manual equals/hashCode (Kotlin data classes don't content-compare ByteArray fields, so a synthesized equals would have silently always been false/reference-equality in tests), and `Framer.feed(bytes): List<Frame>` mirroring tools/dp_receiver.py's proven-on-hardware recovery shape (find sync via scan, wait for the full header+payload+crc to arrive, bad crc drops only the 2 sync bytes and rescans -- not the whole frame). One deliberate improvement over the Python reference: on no-sync-found, a trailing lone 0xD7 is kept (not dropped) in case the second sync byte lands in the next feed() -- a real scenario once this is wired to live USB-CDC chunks, tested by "sync split exactly between the two sync bytes still decodes".
android/core/build.gradle.kts now exposes `-Ddroidputter.fixturesDir=<repo>/fixtures` to the test JVM (`rootProject.projectDir.parentFile.resolve("fixtures")`) so FramerTest can load fixtures/pense-bem/boot.bin without depending on Gradle's implicit working directory.
Two deliberate deviations from this task's literal step wording, both because the wording predates the hardware findings already recorded elsewhere in this ledger (same pattern as task 4's FILL-byte-order note and task 10's own flag on this exact issue):
(1) "a 4097-byte length rejected" describes the shim's native-test-only DP_FRAME_MAX_PAYLOAD=4096 cap (dp_frame.h) -- task 10's note already flagged that cap as smaller than the real wire ceiling. The real ceiling is the u16 length field itself, 65,535 B (docs/PROTOCOL.md), and real captured RECT_RLE frames in fixtures/pense-bem/boot.bin already run 6-10 KB, well past 4096. Enforcing a 4096 cap here would break decoding real hardware fixtures, so DP_MAX_PAYLOAD=0xFFFF is documentation only (a u16 decode can never exceed it, so there is nothing to actively reject); the test that stands in for "oversize rejected" is `a near wire-ceiling payload decodes whole -- no artificial 4096 cap`, a synthetic 64,800 B RECT (S5's measured full-frame size) that must decode intact.
(2) "the S2 fixture decodes to >= 1 HELLO and >= 1 RECT with no framing errors" -- fixtures/pense-bem/boot.bin (re-recorded by task 2's re-verify, see fixtures/README.md) has ZERO plain RECT (type 0x03) frames, only RECT_RLE (type 0x04, Pense-Bem's screen compresses well); and it has 3 resynced framing errors, not 0 (`tools/dp_receiver.py --decode` confirms: counts {STATS: 30, RECT_RLE: 57, HELLO: 3} bad 3, cross-checked against this file's own boot.bin re-recording note). `replays the real S2 fixture boot bin...` asserts the actual byte-exact counts (3/57/30, resyncCount==3) instead of the stale >=1-RECT/zero-errors claim.
Verify: `cd android && JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :core:test` BUILD SUCCESSFUL, test-results XML shows tests="9" skipped="0" failures="0" errors="0" (3 worked-example decodes, split-mid-payload, split-at-sync-boundary, corrupted-crc-then-resync, noise-before-frame, near-ceiling-payload, fixture replay). `:core:koverVerify` (80% line bound) BUILD SUCCESSFUL and `android/gates.sh` still prints "core android-import files: 0" then "gates ok" -- Framer/Frame/dpCrc8 have zero android.* imports, pure JVM as the golden rule requires.
2026-09-02 Task 22 [REAL] Message decoders: android/core/src/main/kotlin/com/droidputter/core/protocol/DpMessage.kt -- sealed `DpMessage` (Hello, Fill, Rect, RectRle, Stats) plus `decodeDpMessage(Frame)` dispatching on type, and one `decode*(payload: ByteArray): X?` per type. Every decoder is a pure bounds check, not a crash surface: HELLO/FILL/STATS reject on wrong fixed payload size; RECT rejects when `w*h*2 != payload.size - 8` (the task's literal example); RECT_RLE rejects when run bytes aren't a multiple of 3, a run has a zero count (would infinite-loop a naive expander), a run would overrun the declared `w*h`, or the runs don't sum exactly to `w*h` -- all return null instead of throwing or writing out of bounds. Rect/RectRle are data classes with manual equals/hashCode (IntArray fields don't get structural equality from the compiler-generated ones, same reason Frame in DpFraming.kt has a manual equals). Pixel byte order: RECT/RECT_RLE big-endian per pixel (panel-bus order, confirmed [REAL] in docs/PROTOCOL.md); FILL's color is little-endian (the documented shim asymmetry) -- handled so callers don't need to know about it.
DpMessageTest.kt (15 tests): HELLO/FILL/RECT/RECT_RLE/STATS worked examples (RECT and FILL have no byte-exact worked example in docs/PROTOCOL.md, so those two are synthetic per the documented byte layout; HELLO, RECT_RLE and STATS are decoded from the real fixture/doc bytes), one bounds-check-rejection test per type (RECT's is the task's literal `w*h*2 != payload size - 8` case), an unknown-type-returns-null test, and two fixture-replay tests against fixtures/pense-bem/boot.bin: every one of its real captured frames decodes (no nulls) and the per-type counts match fixtures/README.md exactly (3 HELLO + 57 RECT_RLE + 30 STATS, same numbers FramerTest already established at the raw-Frame level); the first HELLO's decoded board/app strings ("cardputer-adv"/"app") are asserted byte-exact against the S2 line in this file's own history.
Verify: `cd android && JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew :core:test` BUILD SUCCESSFUL, DpMessageTest test-results XML shows tests="15" skipped="0" failures="0" errors="0". `android/gates.sh` still prints "core android-import files: 0" then "gates ok" (koverVerify 80% line bound passes) -- DpMessage.kt has zero android.* imports.
